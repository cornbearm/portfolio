# 2026-06-28 桌游设计详情优化

- 用户反馈：图片底下不要出现 caption（实物拍摄、真实游玩·规则解读、完整游玩视频·5-8人实测、游戏规则·卡牌设计、灵感·调研问卷·总结）；5 张图 + Vimeo 视频改成按顺序竖排
- 改动：commit `c64e2af`
  - 把 lightindark 的 5 张图 layout 全部从 `hero`/`half` 改为 `full`
  - 把所有 caption（cn/en）置空字符串
  - Vimeo 视频的 caption 也清空，layout 保持 `full`
- 效果：5 张图 + 1 个视频以 full 宽度（整栏宽）从上到下顺序排列，无任何文字标注
- 渲染逻辑：editorial-gallery 里 `eg-full` 是 100% 宽，`eg-half` 是 50% 两列，`eg-hero` 是 100% 宽但有特殊样式。caption 在 `eg-item` 里的 `<figcaption>` 是 `p(item)` 存在时才输出

## 2026-06-28 视频双源：本地 mp4 + Vimeo 跳转

- 用户反馈：Vimeo 在大陆默认访问不到，桌游视频要给国内访客看
- 改动：commit `a6d1d4a`
  - 用 `yt-dlp` 拉 Vimeo 1080p/720p mp4，最终用 720p（31.29MB，1280x720，码率 2.8Mbps）
  - 视频文件保存到 `videos/lightindark.mp4`（GitHub Pages 部署单文件限制 100MB，32MB 完全 OK）
  - Gallery 数据从 `{type:'vimeo', id:'888177658'}` 改成 `{type:'video', src:'videos/lightindark.mp4', vimeoUrl:'...'}`
  - `video` 渲染分支加了 `vimeoUrl` 时输出一个「在 Vimeo 上观看完整版 →」跳转链接
  - 加了 `.eg-vimeo-link` CSS：默认浅色，hover 变深
- 部署后验证：MP4 文件 HTTP 200，content-length 32813353 byte，部署后约 1.5 分钟可访问
- 教训：GitHub Pages 处理大文件（>1MB）需要 1-2 分钟部署时间，要等而不是立刻重试；HEAD 请求返回 200 但无 content-length 时不是 404，是文件正在被缓存
- yt-dlp 工具路径：`/Users/emo/.workbuddy/binaries/python/versions/3.13.12/bin/yt-dlp`
- yt-dlp 用法：`-F` 列格式，`--format "http-720p"` 下载 720p http 直链（避免 HLS/DASH 流）

## 2026-06-28 交互装置：苔息共振 - 地衣（commit 7a2f758）

- 用户提供 5 张 PNG 截图（位置：portfolio-editor/images/交互装置/），内容是「地衣共生循环」交互装置
- 项目概念（来自图 1 文字）：
  - 「地衣共生循环」是一座会呼吸的微型剧场
  - 人点亮光源、滴入养料，地衣便以孢子和隐秘的化学语言回应
  - 这些信号被实时转译为光影、声音与气味，反作用于人的情绪
  - 情绪再调节光照强度、干湿、光频等，与人完成一轮无声的共生循环
  - 让「我即生态」在几分钟内被身体直接感知
- 处理流程：
  1. PNG 转 JPG：Pillow 打开后 `convert('RGB')`，等比 resize 限制宽 2000px
  2. quality=85 + optimize=True，5 张图从 60MB+ 压到 ~1.3MB
  3. 保存到 `images/lichen/1-5.jpg`
- 数据填入：
  - WORKS `i1`：`img: 'images/lichen/1.jpg'`，标题改「交互装置 · 苔息共振」，加 tag「地衣」
  - DETAIL `i1`：subtitle / title / role / timeline=2024 / tools（Photoshop, Illustrator, Blender, Arduino, 3D打印）/ overview / responsibility / outcome，中英双语文案
  - gallery：5 张图全部 `full` 竖排，caption 全部空（沿用 lightindark 风格）
- 部署：home 200 OK，1.jpg 等 30 秒后才返回 200（GitHub Pages CDN 缓存）
- 经验：**用户给文件夹而不是单图时，先 PIL convert+resize+optimize 批量处理**，避免大文件进 Git
- 「full 竖排无 caption」现在是用户偏好的作品详情标准排版

## 2026-06-28 集友趣设计手册：manual.html 在线翻页（commit 8d22d08）

- 用户需求：Google Drive 上的 50 页 PNG 设计手册 → 部署成网页跳转，放在集友趣卡片毕设链接旁边，叫"设计手册"
- 处理流程：
  1. **gdown 拉 Drive 整文件夹**：`/Users/emo/.workbuddy/binaries/python/versions/3.13.12/bin/gdown --folder <URL> -O <dir>`
  2. **实际下载 27 个文件**（不是 50）：0.1封面 + 0.2扉页 + 0.3 + 0/2/4/.../50 画板（PNG 86MB）
  3. **Pillow 批量转 JPG**：等比缩到宽 1600px，quality=82 optimize，**86MB → 3MB**
  4. **重命名顺序**：`0.1封面.png`→01.jpg, `0.2扉页.png`→02.jpg, `0.3.png`→03.jpg, `0_画板`→04.jpg, `2_画板`→05.jpg, `4_画板`→06.jpg, `6_画板`→07.jpg, ..., `50_画板`→27.jpg
- 新增 `manual.html` 翻页器：
  - 顶部 sticky bar：标题「集友趣 · 设计手册」+ 「← 返回作品集」链接
  - 27 页竖排加载，`loading="lazy"` 懒加载
  - 每页右下角显示 `01/27` 之类的页码
  - 暗色背景突出彩色内容
- home.html 改动：
  - 集友趣 links 数组新增 `{ name: '设计手册', icon: '📖', cn: '设计手册', en: 'Design Manual', url: 'manual.html' }`，紧跟在毕设后面
  - 渲染逻辑改成：`/^https?:\/\//.test(lk.url)` → 外链 `target="_blank"`，内链（`manual.html`）同标签页打开
- 部署状态：HOME 200 / MANUAL 200 / 01.jpg 200，27 张图全可访问
- 教训：
  - 用户说「50 页」未必精确，gdown 下来是 27 个 PNG。**先 ls 实际下载数量再确定页面计数**
  - Drive 整文件夹下载用 gdown 的 `--folder` 参数
  - PNG→JPG 批量处理必须转 RGB + 等比缩放 + quality 85 左右 + optimize=True，86MB 压到 3MB 完全可以
  - 内/外链渲染逻辑用 URL 正则区分更自然（`/^https?:\/\//`）

## 2026-06-28 书籍设计：藏于冰川之下 Hidden Under the Glacier（commit 03a456b）
- 用户提供 5 张 PNG 截图（位置：portfolio-editor/images/书籍设计/）
- 关键澄清：用户说「空间装置的作品那一栏」实际应该是**设计分类**（书籍设计归属设计类目），d1 原本就是占位项
- 项目概念（来自图 1 + 图 2 文字）：
  - 「HIDDEN UNDER THE GLACIER / 藏于冰川之下」关于气候危机的概念书籍
  - 通过展示南极磷虾的现状说明气候危机的影响
  - 揭露人类破坏自然的后果，引发对人与自然关系的思考
  - 书名取自莎士比亚《李尔王》
  - 装帧概念：「冰块打破 → 真正的书籍跳出」，书被冰封是视觉隐喻
- 5 张图：概念封面 → 灵感/调研/草图 → 结构/内容草图 → 视频+实物拍摄 → 细节图
- 数据填入：
  - WORKS `d1`：`img: 'images/bookdesign/1.jpg'`，标题改「书籍设计 · 藏于冰川之下」，加 tag「概念」
  - DETAIL `d1`：subtitle / title / role / timeline=2023 / tools / overview / responsibility / outcome，中英双语文案
  - gallery：5 张图全部 `full` 竖排，caption 全部空
- 部署后验证：HOME 200，bookdesign/1.jpg 200
- **判断分类冲突的标准**：用户说 X 分类但内容明显是 Y 分类时，**优先按内容逻辑填到正确分类**（d1 已是占位项），并在回复中明确说明，让用户纠正

## 2026-06-28 交互装置：添加 Vimeo 视频（commit 119ca4f）
- 用户提供 Vimeo 链接：https://vimeo.com/891748593（苔息共振 实测视频）
- 复用 lightindark 的双源方案：
  - `yt-dlp -F` 看格式，`http-720p` 拉 18.36MB mp4（1280x720，2.5Mbps）→ `videos/lichen.mp4`
  - Gallery 末尾追加 `{ type:'video', src:'videos/lichen.mp4', vimeoUrl:'https://vimeo.com/891748593' }`
  - 视频下方自动渲染「在 Vimeo 上观看完整版 →」跳转链接
- 部署验证：HOME 200 / videos/lichen.mp4 200 (content-length: 19249675)
- 经验：**双源方案可复用**：本地 mp4 + vimeoUrl 字段，不需要改 video 渲染分支代码
- 视频文件命名约定：`videos/<项目英文名>.mp4`，跟 images/<项目英文名> 保持一致

## 2026-06-28 衍生品设计：鹅来运转出气系列（commit b998e46）
- 用户提供 5 张剪贴板图（clipboard-2026-06-27T17-28-09-*）
- 5 张图内容（从左到右）：
  1. 毛绒产品 - 出气般 PUNCHING DICE（100×100×100mm 聚酯纤维，红黑企鹅）
  2. 毛绒产品 - 出气包 PUNCHING BAG（300×400×250mm 羽绒棉，白蓝企鹅）
  3. PVC产品 - 出气筒（100×130×20mm PVC，洗衣机造型企鹅）
  4. 鸣潮 - 毛绒团子包装（多角色二次元可爱风）
  5. 包装设计（库洛战双 + 鹅来运转钥匙扣多角色）
- 处理流程：剪贴板 JPG (1920x1080) → 限制宽 2000px（已经是这个尺寸）→ quality=88 → 1.2MB
- 数据填入：
  - WORKS `d3`：`img: 'images/merchandise/1.jpg'`，标题改「衍生品设计 · 鹅来运转解压系列」，加 tag「解压」
  - DETAIL `d3`：subtitle / title / role / timeline=2026 / tools / overview / responsibility / outcome，中英双语文案
  - gallery：5 张图全部 `full` 竖排，caption 全部空
- 部署验证：HOME 200 / 1-5.jpg 200 (30s 后)
- 经验：**用户给剪贴板图时直接用路径 `/Users/emo/.workbuddy/clipboard-images/clipboard-*.jpg`**，不需要再粘贴
- 「full 竖排无 caption」模式继续作为衍生品 / 桌游 / 装置的详情标准排版
- 衍生品设计项目跨多个品牌（腾讯企鹅 / 鸣潮 / 库洛战双），subtitle 注明「多IP包装设计」更准确
